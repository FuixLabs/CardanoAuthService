import { makeStyles } from "@mui/styles";

const styles = makeStyles((theme) => ({
  walletInfoContainer: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    width: "50%",
  },
  walletLogo: {
    marginRight: 20,
  },
}));

export default styles;
